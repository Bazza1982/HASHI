# HASHI standard local runtime.
#
# The minimal package core is declared in pyproject.toml. This source-checkout
# profile additionally keeps the standard media, Remote, and TUI surfaces used
# by the normal launchers. Optional integrations live in named project extras.

# Core runtime
python-telegram-bot>=20.0
httpx>=0.24.0
aiohttp>=3.8.0
PyYAML>=6.0
jsonschema>=4.20.0,<5.0.0
croniter>=1.3.0

# Standard local features: media, Hashi Remote, and TUI
pillow>=12.2.0
PyMuPDF>=1.26.0,<2.0.0
fastapi>=0.115.0
uvicorn>=0.30.0
zeroconf>=0.136.0
cryptography>=45.0.0
rich>=13.0.0
textual>=0.50.0
