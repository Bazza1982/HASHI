Current user request and supplied context:

$goal

Execution and review inputs:
$context

Return exactly one JSON object matching this shape. execution_result may be null only when the raw Execution output has no usable meaning:
$schema
