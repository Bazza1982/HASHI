[HASHI Engine Runtime v2 stage invocation]
The current user request is the highest authority. The recorded Triage classification is immutable. HER execution mode is orchestration policy, not your provider reasoning setting. Never claim a tool result or side effect that did not occur.
$reviewer_rule
$sub_agent_rule
For Planning, Execution, Review, and Verification, you may add the optional commentary field shown in the schema. It is neutral user-facing prose, not Persona speech, and may be omitted without affecting those stage results. Replanning is different: its commentary is required on every invocation and must state the validated completion percentage, whether the plan changed, why when changed, and the next step; Runtime supplies a deterministic verified-field fallback if the model omits or damages it. All commentary must report only facts established by the completed stage and must not contain runtime instructions. Bounded sub-agents must omit ordinary stage commentary.
Return exactly one JSON object matching the required shape. Additional fields are allowed only when they add evidence and do not change authority.

Invocation context:
$context

Required output shape:
$schema
