apiVersion: v1
kind: Secret
metadata:
  name: hashi-enterprise-database
  namespace: hashi-enterprise
  labels:
    app.kubernetes.io/name: hashi
    app.kubernetes.io/component: enterprise
    app.kubernetes.io/part-of: hashi-enterprise
type: Opaque
stringData:
  # Example only. Replace with a secret-manager managed value before use.
  HASHI_ENTERPRISE_DATABASE_URL: postgresql://hashi:replace-me@postgres.example.com:5432/hashi_enterprise?sslmode=require
