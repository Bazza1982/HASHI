# HASHI Portable Windows x64 runtime dependencies.
# The standard runtime lock is the version authority for shared dependencies;
# requirements.lock pins and hashes the complete Windows/Python 3.12 closure.
-c ../../constraints/standard-py312.lock
-r ../../requirements.txt

# Portable-only runtime dependencies.
edge-tts==7.2.7
playwright==1.58.0
psutil==7.2.2
